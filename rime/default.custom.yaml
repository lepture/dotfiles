# Rime default settings
# encoding: utf-8

patch:
    schema_list:
      - schema: luna_pinyin
      - schema: wubi_pinyin
      - schema: wubi86

    switcher:
      caption: Input Method
      hotkeys:
        - 'Control+s'
        - 'Control+Shift+s'
        - F4

    menu:
      page_size: 5
