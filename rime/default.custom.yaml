# Rime default settings
# encoding: utf-8

patch:
    schema_list:
      - schema: wubi_pinyin
      - schema: wubi86
      - schema: luna_pinyin

    switcher/hotkeys:
        - 'Control+space'

    menu/page_size: 6
